import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Header = () => {
  return (
    <View style={{}}>
      <Text style={{color:"red" , textAlign:"right", padding:10, fontSize:20}}>0</Text>
    </View>
  )
}

export default Header

const styles = StyleSheet.create({})